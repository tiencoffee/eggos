colrs = {
	light: #eee #ddd #ccc #aaa4 #7774
	blue: #17b #16a #168 #17b2 #17b4
	red: #d33 #b33 #a33 #d332 #d334
}
for c, colr in colrs
	for val, i in colr
		define(c + i, val)
box-orient()
	-webkit-box-orient arguments
	box-orient arguments
font-smoothing()
	-webkit-font-smoothing arguments
	font-smoothing arguments
line-clamp()
	-webkit-line-clamp arguments
	line-clamp arguments
text-security()
	-webkit-text-security arguments
	text-security arguments
user-drag()
	-webkit-user-drag arguments
	user-drag arguments
user-modify()
	-webkit-user-modify arguments
	user-modify arguments
text-truncate(line)
	if line < 2
		text-overflow ellipsis
		white-space nowrap
		overflow hidden
	else
		display -webkit-box
		line-clamp line
		box-orient vertical
		overflow hidden

// @import '//cdn.jsdelivr.net/gh/tiencoffee/fonts/Calibri/Calibri.css'
@import '//fonts.googleapis.com/css?family=Verdana:400,700'
body
	margin 0
	font 12pt 'Verdana',sans-serif
	word-break break-word
	overflow hidden
	user-select none
	cursor default
*, *:before, *:after
	box-sizing border-box
	vertical-align middle
:focus
	outline 0
::selection
	background blue0
	color #fff
::-webkit-scrollbar
	width 12px
	height 12px
::-webkit-scrollbar-track
	background light0
::-webkit-scrollbar-thumb
	background blue0
h1, h2, h3, h4, h5, h6, p
	margin 8px 0
img, a
	user-drag none
a, abbr, acronym, b, bdo, big, br, cite, code, dfn, em, i,
kbd, map, q, samp, small, span, strong, sub, sup, time, tt, var
	vertical-align baseline

#appEl
	position absolute
	inset 0
	&:empty
		display none

.Icon-fa
	display inline-flex
	justify-content center
	align-items center
.Icon-img
	width 18px
	height 18px
	object-fit cover

.Button
	height 28px
	padding 0 4px
	border solid 1px #0004
	border-radius 0
	display inline-flex
	justify-content center
	align-items center
	font inherit
	white-space nowrap
for c, colr in colrs
	isLight = c is 'light'
	.Button-{c}
		background colr[0]
		color isLight ? #000 : #fff
		&:hover
			background colr[1]
		&:active
			background colr[2]
		&.Button-minimal
			color isLight ? #000 : colr[0]
			&:hover
				background colr[3]
			&:active
				background colr[4]
.Button-minimal
	border-color transparent
	background transparent
.Button_text
	margin 0 4px
.Button_icon
	margin 0 4px

.TextInput
	position relative
	height 28px
	border solid 1px #0004
	border-radius 0
	display inline-flex
	justify-content center
	align-items center
	background #fff
	box-shadow inset 0 1px 2px #0001
	&:focus-within
		border-color blue0
		box-shadow 0 0 0 1px blue0
		z-index 1
	> .Button
		height 100%
		margin 2px
		border 0
.TextInput_leftIcon
	padding-left 8px
.TextInput_rightIcon
	padding-right 8px
.TextInput_input
	width 100%
	height 100%
	padding 0 8px
	border 0
	border-radius 0
	font inherit
	background transparent

.PasswordInput-hidePassword
	> .TextInput_input
		text-security square

.Radio
.Checkbox
	height 28px
	display inline-flex
	align-items center
.Radio_input
.Checkbox_input
	position relative
	width 18px
	height 18px
	margin 0
	border solid 1px #0004
	background light0
	appearance none
	&:hover
		background light1
		&:checked
			background blue1
	&:active
		background light2
		&:checked
			background blue2
.Radio_input
	border-radius 50%
	&:checked
		background blue0
		&:after
			position absolute
			inset 4px
			border-radius 50%
			background #fff
			content ''
.Checkbox_input
	border-radius 0
	&:checked
		background blue0
		&:after
			position absolute
			inset 0
			display flex
			justify-content center
			align-items center
			font-family 'Font Awesome 5 Pro'
			font-weight 900
			color #fff
			content '\f00c'
.Radio_label
.Checkbox_label
	margin-left 8px

.Select
	position relative
	height 28px
	padding 0 8px
	border solid 1px #0004
	display inline-flex
	align-items center
	background light0
	&:hover
		background light1
.Select_text
	margin-right 8px
	flex-grow 1
	text-truncate 1
.Select_list
	border solid 1px #0004
	background #fff
	overflow-x hidden
	overflow-y auto
	box-shadow 0 2px 8px #0004
	&:empty
		display none
.Select_item
	min-height 28px
	padding 2px 8px
	display flex
	align-items center
.Select_item-hover
	background light1
.Select_divider
	margin 4px 0
	border-bottom solid 1px #0004

.Menu
	padding 4px 0
	border solid 1px #0004
	display inline-block
	background #fff
.Menu_item
	min-height 28px
	padding 2px 8px
	display flex
	align-items center
.Menu_item-hover
	background light1
.Menu_itemIcon
	min-width 20px
	margin-right 8px
	display flex
	justify-content center
	align-items center
.Menu_itemText
	margin-right 16px
	flex-grow 1
.Menu_itemLabel
	font-size 10pt
.Menu_divider
	margin 4px 0
	border-bottom solid 1px #0004
.Menu-popper
	&:empty
		display none
.Menu-submenu
	box-shadow 0 2px 8px #0004

.ControlGroup
	display inline-flex
	justify-content center
	align-items center
	> :not(:first-child)
		margin-left -1px

.Table
	display block
	overflow auto
	tr
		height 28px
	td, th
		padding 0 8px
.Table-hasHeader
	tr:first-child
		text-align left
		th
			position sticky
			top 0
			background #fff
			box-shadow 0 1px #0004
.Table-bordered
	td, th
		&:not(:last-child)
			border-right solid 1px #0004
	tr
		&:not(:last-child)
			border-bottom solid 1px #0004
.Table-striped
	tr
		&:nth-child(even)
			background light0
.Table-interactive
	&.Table-hasHeader tr:not(:first-child)
	&:not(.Table-hasHeader) tr
		&:hover
			background light1
		&:active
			background light2
.Table-content
	width 100%
	border-collapse collapse

.TreeView-subtree
	padding-left 16px
	border-left dotted 1px #0004
.TreeView_nodeBody
	position relative
	height 28px
	display flex
	align-items center
.TreeView_nodeIcon
	height 100%
.TreeView_nodeName
	position absolute
	left 24px
	right 0
	height 100%
	display flex
	flex-grow 1
	align-items center
	overflow hidden

.Dialog
	position absolute
	display flex
	flex-direction column
	background light0
	box-shadow 0 0 0 1px #0004,0 2px 16px #0004
	animation-duration .25s
	animation-timing-function ease
	animation-fill-mode forwards
.Dialog-maximize
	animation-name Dialog-maximize
@keyframes Dialog-maximize
	to
		left 0
		top 0
		width 100%
		height 100%
.Dialog-unmaximize
	animation-name Dialog-unmaximize
@keyframes Dialog-unmaximize
	from
		left 0
		top 0
		width 100%
		height 100%
.Dialog_header
	padding 4px
	display flex
	align-items center
	> .Button
		height 24px
.Dialog_title
	height 100%
	padding 0 8px
	display flex
	flex-grow 1
	align-items center
	justify-content center
	overflow hidden
.Dialog_body
	border 0
	flex-grow 1
	background #fff
.disabled
	opacity .5
	pointer-events none

((styl))

.row
	display flex
	flex-direction row
	> .col
		min-width 0
.column
	display flex
	flex-direction column
	> .col
		min-height 0
.col
	flex-grow 1
.col-0
	flex-shrink 0
for i in range(1, 12)
	val = floor(i / 12 * 100%, 2)
	:not(.column) > .col-{i}
		width val
	.column > .col-{i}
		height val
	.row-{i}
		display grid
		grid-template-columns repeat(i, 1fr)
		> .col-{i}
			grid-column span (i)
	.offset-{i}
		margin-right val
.full
	position absolute
	inset 0
.wrap
	flex-wrap wrap
.nowrap
	flex-wrap nowrap
for dir in Row Column
	.{dir}
		prop = dir is Row ? 'justify-content' : 'align-items'
		&.start
			{prop} flex-start
		&.center
			{prop} center
		&.end
			{prop} flex-end
		prop = dir is Row ? 'align-items' : 'justify-content'
		&.top
			{prop} flex-start
		&.middle
			{prop} center
		&.bottom
			{prop} flex-end
.between
	justify-content space-between
.around
	justify-content space-around
.evenly
	justify-content space-evenly
.stretch
	align-items stretch
props = {p: padding, m: margin}
sides = {t: top, r: right, b: bottom, l: left}
sizes = 32px 16px 8px 4px 2px 0
for p, prop in props
	for size, i in sizes
		i = 5 - i
		negate = p is 'm' and i
		.{p}-{i}
			{prop} size
		if negate
			.-{p}-{i}
				{prop} -(size)
		.{p}x-{i}
			{prop}-left size
			{prop}-right size
		.{p}y-{i}
			{prop}-top size
			{prop}-bottom size
		if negate
			.-{p}x-{i}
				{prop}-left -(size)
				{prop}-right -(size)
			.-{p}y-{i}
				{prop}-top -(size)
				{prop}-bottom -(size)
		for s, side in sides
			.{p}{s}-{i}
				{prop}-{side} size
			if negate
				.-{p}{s}-{i}
					{prop}-{side} -(size)
for size, i in sizes
	i = 5 - i
	.gap-{i}
		gap size
	.gap-x-{i}
		column-gap size
	.gap-y-{i}
		row-gap size
for pos in absolute relative fixed
	.{pos}
		position pos
for size in 100 75 50 25 0
	.w-{size}
		width (size)%
	.h-{size}
		height (size)%
props = {w: width, h: height}
for p, prop in props
	for type in fit min max
		.{p}-{type}
			{prop} (type)-content
for i in 0..5
	.z-{i}
		z-index i
	if i
		.-z-{i}
			z-index -(i)
for dsp in block inline-block inline flex inline-flex
	.{dsp}
		display dsp
.hidden, [hidden]
	display none
for align in left center right
	.text-{align}
		text-align align
.text-baseline
	vertical-align baseline
.text-bold
	font-weight bold
.text-italic
	font-style italic
.text-wrap
	white-space normal
.text-nowrap
	white-space nowrap
.text-pre-wrap
	white-space pre-wrap
.text-pre
	white-space pre
.text-truncate
	text-truncate 1
.text-truncate-2
	text-truncate 2
.text-truncate-3
	text-truncate 3
.text-select
	user-select auto
.text-noselect
	user-select none
.text-select-all
	user-select all
.text-security
	text-security square
.text-small, small
	font-size 10pt
.scroll
	overflow auto
.scroll-y
	overflow-x hidden
	overflow-y auto
.scroll-overlay
	overflow overlay
.scroll-overlay-y
	overflow-x hidden
	overflow-y overlay
.scroll-overlay
.scroll-overlay-y
	&::-webkit-scrollbar-track
	&::-webkit-scrollbar-thumb
		background transparent
	&:hover
		&::-webkit-scrollbar-track
			background light3
		&::-webkit-scrollbar-thumb
			background blue4
.noscroll
	overflow hidden
.event
	pointer-events auto
.noevent
	pointer-events none
.event-childs
	pointer-events none
	> *
		pointer-events auto
for ratio in (1 1) (4 3) (3 2) (5 3) (16 9)
	w = ratio[0]
	h = ratio[1]
	.ratio-{w}x{h}
		aspect-ratio w\/h
for fit in cover contain scale-down fill none
	.img-{fit}
		object-fit fit
.img-contrast
	image-rendering -webkit-optimize-contrast

props = {bg: background, text: color}
for p, prop in props
	for c, colr in colrs
		.{p}-{c}
		.hover-{p}-{c}
		.active-{p}-{c}
			{prop} colr[0]
